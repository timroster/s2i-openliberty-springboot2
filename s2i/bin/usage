#!/bin/bash -e
cat <<EOF
This is the s2i-openliberty-springapp S2I image:
To use it, install S2I: https://github.com/openshift/source-to-image

Sample invocation:

s2i build <source code path/URL> s2i-openliberty-springapp <application image>

You can then run the resulting image via:
docker run <application image>
EOF
